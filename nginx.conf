user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

server {
  server_name lsparlin-apex.up.railway.app/;
  rewrite ^/(.*)$ https://www.lsparlin.dev/$1 permanent;
}
